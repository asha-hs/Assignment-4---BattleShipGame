import static org.junit.Assert.*;

import org.junit.Before;
import org.junit.Test;

public class ShipTest {

    @Before
    public void setUp() throws Exception {
	
	
    }

    @Test
    public void testOkToPlaceShip() {
	Ocean ocean = new Ocean();
	/*
	Ship battleShip = new BattleShip();
	boolean isOkToPlaceShip = battleShip.okToPlaceShip(0, 0, true, ocean);
	if(isOkToPlaceShip)
	    battleShip.placeShipAt(0, 0, true, ocean);
	assertEquals(true, isOkToPlaceShip);
	Ship battleCruiser = new BattleCruiser();
	isOkToPlaceShip = battleCruiser.okToPlaceShip(1, 1, false, ocean);
	if(isOkToPlaceShip)
	    battleCruiser.placeShipAt(1, 1, false, ocean);
	assertEquals(true, isOkToPlaceShip);
	*/
    }
    

}
